# Copier vers .env et remplir avec le Connect modal Supabase (Vite / React).
# Nom attendu par Supabase UI + shadcn : VITE_SUPABASE_PUBLISHABLE_KEY
# (le dashboard peut afficher « default publishable key » — c’est la même valeur.)

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
